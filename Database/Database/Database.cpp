#include <iostream>
#include <vector>
#include <map>
#include <unordered_map>
#include "Table.h"



int main() {

    try {
       /*
        Create you database or use one below. 
        You can perform some basic operations on it as presented below.

        Table table1;
        Table table2;
        table1.addColumn("Name");
        table1.addColumn("Age");
        table1.addColumn("Country");
        table1.addRow({ "Juan", "20", "Spain" });
        table1.addRow({ "Hannah", "21", "Germany" });
        table1.addRow({ "Witold", "24", "Poland" });
        table1.addRow({ "Victor", "26", "Czech" });
        table1.addRow({ "Michael", "35", "USA" });
        table1.displayData();
        table1.alterData(4, "Name", "Arthur");
        table1.displayData();
        table1.dynamicSearch("Age > 21");
        table1.safeToFile("file.txt");
        table2.importFromFile("data.txt");
        */
    }
    catch (const std::runtime_error& e) {
        std::cout << e.what();
    }  
}


